import { StyleSheet, View } from "react-native";

export const dropdownGlobalStyles = StyleSheet.create({
  containerStyle: { width: 30, padding: 0 },
  style: {
    backgroundColor: "transparent",
    borderWidth: 0,
    paddingVertical: 0,
    paddingRight: 0,
    minHeight: 20,
  },
  dropDownContainerStyle: {
    backgroundColor: "transparent",
    borderWidth: 0,
  },
  textStyle: { color: "white", fontSize: 20 },
  listItemContainerStyle: { height: 25 },
});
